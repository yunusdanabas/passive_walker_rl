# Configuration Directory

This directory contains configuration files for different aspects of the project.

## Structure

- `training/` - Training configuration files
- `evaluation/` - Evaluation configuration files  
- `environment/` - Environment configuration files

## Usage

Configuration files define default parameters and settings for various components of the system.
